Prerequisites:

1. Follow the 'environment setup' guide from React Native: https://reactnative.dev/docs/environment-setup

Steps to execute this project on the first time:

1. Execute the 'npm i && npm run-script android' or 'npm i && npm run-script ios' command to run the project on specific platform.